{
  "name": "college-management-system",
  "private": true,
  "version": "1.0.0",
  "description": "Render deployment entrypoint for the College Management System monorepo",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install:backend": "npm ci --include=dev --prefix backend",
    "install:frontend": "npm ci --include=dev --prefix frontend",
    "build": "npm run install:backend && npm run install:frontend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  }
}
